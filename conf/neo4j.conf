# ~/neo4j/conf/neo4j.conf
# A second, minimal config file used by neo4j_manager.py when it runs
# `neo4j-admin load` / `neo4j-admin dump` in a throw‑away container.
# Keep it in sync with project/neo4j.conf where it matters.

# JVM option required by the GDS plugin on macOS
server.jvm.additional=-Djol.skipHotspotSAAttach=true

# Memory
server.memory.pagecache.size=512M

# Networking
server.default_listen_address=0.0.0.0

# Plugin / log / import directories
server.directories.plugins=/plugins
server.directories.logs=/logs
server.directories.import=/import

# Allow unrestricted GDS procedures (Community Edition)
dbms.security.procedures.unrestricted=gds.*

# Disable anonymous usage data sent to Neo4j
dbms.usage_report.enabled=false
